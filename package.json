{
  "name": "simple_rest_api",
  "version": "0.0.1",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc --outDir dist main.ts && cp index.html dist/ && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "psh": "git add -A && git commit -m \"saved by script\" && git push"
  },
  "author": "jk",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
